import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#2D2A29',
        },
        secondary: {
            main: '#DF7046',
        },
        background: {
            default: '#D9E8D6',
        }
    },
});

